Django>=5.2
python-decouple>=3.8
psycopg[binary]>=3.1
httpx>=0.27
# The socialaccount extra carries requests/oauthlib, which the GitHub OAuth2
# flow needs at runtime; the bare package installs without them.
django-allauth[socialaccount]>=65.0
